# ------------------------------------------------------------------------
# Copyright (c) 2022 megvii-model. All Rights Reserved.
# ------------------------------------------------------------------------
# Modified from BasicSR (https://github.com/xinntao/BasicSR)
# Copyright 2018-2020 BasicSR Authors
# ------------------------------------------------------------------------
# general settings
name: RestoreDiNAT-SPDD-width48-test
model_type: ImageRestorationModel
scale: 1
num_gpu: 1
manual_seed: 10

datasets:
  test:
    name: spdd-val
    type: PairedImageDataset
    dataroot_lq: ./datasets/DPDD/test/inputC.lmdb
    dataroot_gt: ./datasets/DPDD/test/target.lmdb
    io_backend:
      type: lmdb

network_g:
  type: RestoreDiNATLocal
  inp_channels: 3
  out_channels: 3
  dim: 48
  chan_adapt: True
  mode: hybrid

# path
path:
  pretrain_network_g: ./experiments/RestoreDiNAT-SPDD-width48/models/net_g_140000.pth
  strict_load_g: true
  resume_state: ~

# validation settings
val:
  save_img: true
  grids: false


  metrics:
    psnr: # metric name, can be arbitrary
      type: calculate_psnr
      crop_border: 0
      test_y_channel: false
    ssim:
      type: calculate_ssim
      crop_border: 0
      test_y_channel: false

# dist training settings
dist_params:
  backend: nccl
  port: 29500
